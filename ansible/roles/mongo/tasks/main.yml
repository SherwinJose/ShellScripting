- name: Copy MongoDb Repo
  ansible.builtin.copy:
    src: mongo.repo
    dest: /etc/yum.repos.d/mongo.repo

- name: intalling MongoDB
  ansible.builtin.yum:
    name: mongo-db.org
    state: installed